# Prepared project

~~~ bash
mkdir react-demo; cd react-demo
npm init -y
npm install --save-dev webpack
npm install --save-dev babel-core babel-loader webpack-dev-server babel-preset-es2015 babel-preset-es2016 babel-preset-es2017 babel-polyfill
npm install --save-dev babel-preset-react react react-dom react-bootstrap

mkdir build
# edit build/index.html
mkdir app
# edit app/index.js

# edit webpack.config.js

# adapt package.json

npm run build
~~~


# Run project

~~~ bash
npm start
~~~
